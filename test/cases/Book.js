import React from 'react';
import Book from '../../src/components/Book/Book';
import skipFrame from '../utils/skipFrame';
import books from '../utils/books.json';

describe('Book', () => {
	it('should pass right books as property');
	it('should not show books that is not selected by users');
	it('should render books with right title and author when available');
	it('should update a book when select an action');
	it('should remove a book when select none')
});
