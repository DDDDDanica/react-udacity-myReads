import React from 'react';
import BookShelf from '../../src/components/BookShelf/BookShelf';
import skipFrame from '../utils/skipFrame';
import books from '../utils/books.json';

describe('BookShelf', () => {
	it('should pass right books and shelf as property');
	it('should render books there is books');
	it('should render nothing when there is no books');
});